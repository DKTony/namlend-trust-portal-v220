# NamLend Trust - Flow Validation Matrix

**Doc Revision**: 2026-04-06
**Status**: Active (Working Document)

---

## Purpose

Track action-level conformance between implemented behavior and `docs/FLOWS.md`.

---

## Status Legend

| Status        | Meaning                                            |
| ------------- | -------------------------------------------------- |
| `not_started` | Not yet reviewed                                   |
| `in_progress` | Being validated                                    |
| `pass`        | Matches documented flow                            |
| `fail`        | Does not match documented flow                     |
| `blocked`     | Cannot validate due to environment/data dependency |

---

## Flow-Level Baseline

| Flow ID | Flow Name                   | Primary Route(s)                         | Existing Evidence                                                                                               | Status        | Notes                                                                                       |
| ------- | --------------------------- | ---------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------- |
| LF-01   | Loan Application            | `/loan-application`                      | `e2e/loan-application.e2e.ts`, `docs/FLOW_VALIDATION_REPORT_2026-02-14.md`                                      | `blocked`     | Submission scenario skipped due KYC precondition in current test context                    |
| LF-02   | Admin Review and Approval   | `/admin/approvals`                       | `e2e/admin-approvals.e2e.ts`, `e2e/admin-approvals-actions.e2e.ts`, `docs/FLOW_VALIDATION_REPORT_2026-02-14.md` | `pass`        | UI approval controls and routing path passed in current cluster                             |
| LF-03   | Disbursement                | `/admin/loans`, `/admin/disbursements`   | `e2e/ipp-lifecycle.e2e.ts`, `docs/IPS_TESTING.md`                                                               | `pass`        | Current lifecycle spec verifies admin IPS disbursement inside the full loan/payment journey |
| LF-04   | Payments                    | `/payment`, `/loans/:id`                 | `e2e/ipp-lifecycle.e2e.ts`, `docs/IPS_TESTING.md`                                                               | `pass`        | Current lifecycle spec verifies client IPS repayment and post-payment admin verification    |
| LF-05   | Collections                 | `/admin/payments` collections surfaces   | `e2e/api/api-collections.e2e.ts`, `docs/FLOW_VALIDATION_REPORT_2026-02-14.md`                                   | `fail`        | Post-throttled rerun shows deterministic API contract/status failures (not auth-noise)      |
| LF-06   | Notifications               | Header popover and notification surfaces | `e2e/api/api-notifications.e2e.ts`, `docs/FLOW_VALIDATION_REPORT_2026-02-14.md`                                 | `fail`        | Post-throttled rerun shows deterministic `404/500` and role-negative status mismatches      |
| LF-07   | Settlement                  | `/admin/reconciliation`                  | `e2e/api/api-reconciliation.e2e.ts`, `docs/FLOW_VALIDATION_REPORT_2026-02-14.md`                                | `fail`        | Rerun isolates deterministic role-negative mismatch (`expected 403`, `received 401`)        |
| LF-08   | Audit and Compliance        | Admin audit surfaces                     | `e2e/api/api-audit.e2e.ts`, `docs/FLOW_VALIDATION_REPORT_2026-02-14.md`                                         | `fail`        | Rerun isolates deterministic role-negative mismatch (`expected 403`, `received 401`)        |
| LF-09   | Budget and Finance Tracking | `/budget`                                | `FLOW-FIX-BUDGET-001/002/003` implemented in `fix/responsive-ui-enhancements`                                   | `in_progress` | All 3 no-op CTAs fixed; remaining steps (LF-09-S01, S02) still need validation              |

---

## Action-Level Validation Table (Auto-Populated from Code)

| Flow ID | Step ID   | Role               | Screen/Route                | UI Element (test id/text)                              | Handler Function                                                                                                                                    | Intent Statement                                          | Expected Next Screen/Phase                | Expected Backend Operation                                                     | Validation Method | Evidence                                                              | Status        | Notes                                                                                            |
| ------- | --------- | ------------------ | --------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------ | ----------------- | --------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------ |
| LF-01   | LF-01-S01 | client             | `/loan-application`         | KYC warning CTA (`Go to KYC Verification`)             | inline `onClick={() => navigate('/kyc')}` in `src/pages/LoanApplication.tsx`                                                                        | Redirect ineligible users to KYC completion               | KYC screen                                | none                                                                           | static + e2e      | code mapping                                                          | `not_started` | Validate guard appears only when eligibility fails                                               |
| LF-01   | LF-01-S02 | client             | `/loan-application`         | Step next button (`Continue`)                          | inline `onClick={() => setStep(step + 1)}` in `src/pages/LoanApplication.tsx`                                                                       | Advance application wizard                                | next form step                            | none                                                                           | static + manual   | code mapping                                                          | `not_started` | Validate required fields gate progression                                                        |
| LF-01   | LF-01-S03 | client             | `/loan-application`         | Submit button (`handleSubmit`)                         | `handleSubmit` in `src/pages/LoanApplication.tsx` -> `submitApprovalRequest` in `src/services/approvalWorkflow.ts`                                  | Submit loan for approval workflow                         | return to dashboard after success         | `approval_requests` insert                                                     | e2e + DB check    | `e2e/loan-application.e2e.ts` (skipped in Phase 4)                    | `blocked`     | No direct loan creation should occur at submit                                                   |
| LF-02   | LF-02-S01 | admin              | `/admin/approvals`          | Request card click                                     | inline `onClick={() => setSelectedRequest(request)}` in `src/pages/AdminDashboard/components/ApprovalManagement/ApprovalManagementDashboard.tsx`    | Open request details/action panel                         | selected request detail panel             | none                                                                           | static + e2e      | `e2e/admin-approvals.e2e.ts`                                          | `pass`        | Selection and details panel path verified                                                        |
| LF-02   | LF-02-S02 | admin              | `/admin/approvals`          | Approve (`approvals-approve-btn`)                      | `handleStatusUpdate(...,'approved')` in `src/pages/AdminDashboard/components/ApprovalManagement/ApprovalManagementDashboard.tsx`                    | Approve request and create downstream entities            | request processed state                   | `update_approval_status` + `process_approval_transaction`                      | e2e + API + DB    | `e2e/admin-approvals-actions.e2e.ts` + API race test pending rerun    | `in_progress` | Phase 4 validated controls, not full mutation path                                               |
| LF-02   | LF-02-S03 | admin              | `/admin/approvals`          | Reject (`approvals-reject-btn`)                        | `handleStatusUpdate(...,'rejected')` in `src/pages/AdminDashboard/components/ApprovalManagement/ApprovalManagementDashboard.tsx`                    | Reject request with notes                                 | request processed state                   | `update_approval_status`                                                       | e2e + DB          | `e2e/admin-approvals-actions.e2e.ts`                                  | `in_progress` | Requires mutation-assertive run                                                                  |
| LF-02   | LF-02-S04 | admin              | `/admin/approvals`          | Request info (`approvals-requestinfo-btn`)             | `handleStatusUpdate(...,'requires_info')` in `src/pages/AdminDashboard/components/ApprovalManagement/ApprovalManagementDashboard.tsx`               | Return request to applicant for additional data           | requires-info state                       | `update_approval_status`                                                       | e2e + DB          | code mapping                                                          | `not_started` | Add explicit test case                                                                           |
| LF-03   | LF-03-S01 | admin              | `/admin/loans`              | Disburse CTA (`disburse-loan-*`)                       | `handleDisburse` in `src/pages/AdminDashboard/components/LoanManagement/LoanApplicationsList.tsx`                                                   | Prepare pending disbursement and open completion modal    | disbursement modal open                   | `api.disbursements.initiateDisbursement`                                       | e2e + DB          | `e2e/ipp-lifecycle.e2e.ts`                                            | `pass`        | Current lifecycle spec verifies the Convex disbursement preparation path                         |
| LF-03   | LF-03-S02 | admin              | disbursement modal          | Payment method buttons (`payment-method-*`)            | `setPaymentMethod` in `src/pages/AdminDashboard/components/PaymentManagement/CompleteDisbursementModal.tsx`                                         | Choose disbursement channel                               | channel-specific form section             | none until submit                                                              | e2e               | `e2e/backoffice-disbursement.e2e.ts`                                  | `pass`        | Method selection path passed in focused rerun                                                    |
| LF-03   | LF-03-S03 | admin              | disbursement modal          | Complete disbursement (`complete-disbursement-button`) | `handleSubmit` in `src/pages/AdminDashboard/components/PaymentManagement/CompleteDisbursementModal.tsx`                                             | Complete manual disbursement and schedule repayment       | modal closes, loan status updated         | `api.disbursements.completeDisbursement` + ledger outbox event                 | e2e + DB + audit  | `e2e/backoffice-disbursement.e2e.ts`                                  | `pass`        | Manual non-IPP completion path still passes on the current Convex flow                           |
| LF-03   | LF-03-S04 | admin              | disbursement modal (IPS)    | IPS form submit (`Disburse Now`)                       | `handleSubmit` in `src/components/ips/IPSDisbursementForm.tsx` via `useIPSDisbursement`                                                             | Execute instant IPS disbursement                          | processing -> result state                | `api.ips.ipsTransactions.initiateIpsTransaction` + IPS adapter callback path   | e2e + API         | `e2e/ipp-lifecycle.e2e.ts`                                            | `pass`        | Verified in the current end-to-end lifecycle run                                                 |
| LF-04   | LF-04-S01 | client             | `/payment`                  | Pay button (`Pay N$...`)                               | `handlePayment` in `src/pages/Payment.tsx`                                                                                                          | Create payment request from client page                   | success toast, back to dashboard          | `create_payment` RPC                                                           | e2e + DB          | code mapping                                                          | `not_started` | Non-IPS payment page still needs dedicated test                                                  |
| LF-04   | LF-04-S02 | client             | `/loans/:id`                | IPS pay CTA (`ips-payment-button`)                     | inline `onClick={() => setShowIPSModal(true)}` in `src/pages/LoanDetails.tsx`                                                                       | Open IPS repayment flow from loan details                 | IPS payment modal                         | none until modal submit                                                        | e2e               | `e2e/ips-payment-flow.e2e.ts`                                         | `pass`        | Phase 4 verified visibility/open                                                                 |
| LF-04   | LF-04-S03 | client             | IPS payment modal           | Continue buttons (`ips-continue-btn`)                  | `handleNext` in `src/components/ips/IPSPaymentModal.tsx`                                                                                            | Enforce stepwise validation (amount -> VPA -> confirm)    | move to next modal step                   | none                                                                           | e2e               | `e2e/ips-payment-flow.e2e.ts`                                         | `pass`        | VPA and progression checks passed                                                                |
| LF-04   | LF-04-S04 | client             | IPS payment modal           | Pay now (`ips-pay-now-btn`)                            | `handleSubmit` in `src/components/ips/IPSPaymentModal.tsx` via `useIPSRepayment`                                                                    | Execute IPS repayment                                     | processing -> result                      | `api.ips.ipsTransactions.initiateIpsTransaction` + IPS adapter callback path   | e2e + API         | `e2e/ipp-lifecycle.e2e.ts`                                            | `pass`        | Verified in the current end-to-end lifecycle run                                                 |
| LF-04   | LF-04-S05 | client             | dashboard payment modal     | Confirm payment (`handlePayment`)                      | `handlePayment` in `src/components/PaymentModal.tsx` -> `processLoanPayment` in `src/services/paymentService.ts`                                    | Process direct repayment and update loan balance          | success screen/result state               | `process_loan_payment` RPC + schedules + ledger outbox                         | e2e + DB          | code mapping                                                          | `not_started` | Add focused UI test                                                                              |
| LF-04   | LF-04-S06 | client             | `/payment`                  | IPS option (default payment method state)              | `setPaymentMethod('ips')` initial state in `src/pages/Payment.tsx`                                                                                  | Default payment method should already be IPS              | IPS modal opens from current payment page | should call IPS repayment path                                                 | static + e2e      | `e2e/ipp-lifecycle.e2e.ts`                                            | `pass`        | Verified after aligning page state with the visible IPS default                                  |
| LF-05   | LF-05-S01 | admin/loan_officer | collections dashboard       | Refresh                                                | `onClick={() => fetchData(true)}` in `src/pages/AdminDashboard/components/CollectionsManagement/CollectionsDashboard.tsx`                           | Reload queue and metrics                                  | refreshed queue/cards                     | `collectionsAPI.getQueue` + `analyticsAPI.getCollectionsStats`                 | e2e + API         | `e2e/api/api-collections.e2e.ts`                                      | `fail`        | Queue response contract mismatch (`data.queue` expected, array returned)                         |
| LF-05   | LF-05-S02 | admin/loan_officer | collections dashboard       | Log Contact button                                     | inline `setSelectedItem` + `setShowInteractionDialog(true)` in `src/pages/AdminDashboard/components/CollectionsManagement/CollectionsDashboard.tsx` | Open interaction logging dialog                           | interaction dialog open                   | none until submit                                                              | static + manual   | code mapping                                                          | `not_started` | UI dialog flow not yet tested                                                                    |
| LF-05   | LF-05-S03 | admin/loan_officer | interaction dialog          | Log Interaction submit                                 | `handleInteractionSubmit` in `src/pages/AdminDashboard/components/CollectionsManagement/CollectionsDashboard.tsx`                                   | Record collection attempt/outcome                         | dialog closes and queue refreshes         | `collectionsAPI.recordInteraction` (`record_collection_activity`)              | API + DB          | `e2e/api/api-collections.e2e.ts`                                      | `fail`        | Role-negative path mismatch (`expected 403`, `received 401`)                                     |
| LF-05   | LF-05-S04 | admin/loan_officer | collections dashboard       | Record PTP button                                      | inline `setPtpAmount` + `setShowPTPDialog(true)` in `src/pages/AdminDashboard/components/CollectionsManagement/CollectionsDashboard.tsx`            | Open promise-to-pay dialog prefilled with monthly payment | PTP dialog open                           | none until submit                                                              | static + manual   | code mapping                                                          | `not_started` | UI dialog path pending                                                                           |
| LF-05   | LF-05-S05 | admin/loan_officer | PTP dialog                  | Record Promise submit                                  | `handlePTPSubmit` in `src/pages/AdminDashboard/components/CollectionsManagement/CollectionsDashboard.tsx`                                           | Persist promise-to-pay commitment                         | dialog closes and queue refreshes         | `collectionsAPI.createPromise` (`create_promise_to_pay`)                       | API + DB          | `e2e/api/api-collections.e2e.ts`                                      | `fail`        | Reminder endpoints return `500` in current backend                                               |
| LF-06   | LF-06-S01 | client/admin       | notification popover        | Bell icon open                                         | `open` state + `fetchNotifications` in `src/components/NotificationCenter.tsx`                                                                      | Load notifications on demand                              | tabbed notification panel                 | `notificationsAPI.list`                                                        | e2e + API         | `e2e/api/api-notifications.e2e.ts`                                    | `fail`        | Preferences route returns `404` and send flows return `500`                                      |
| LF-06   | LF-06-S02 | client/admin       | notification popover        | Mark all read                                          | `handleMarkAllRead` in `src/components/NotificationCenter.tsx`                                                                                      | Clear unread badge and mark all notifications read        | unread count to 0                         | `notificationsAPI.markAllRead`                                                 | API + DB          | `e2e/api/api-notifications.e2e.ts`                                    | `fail`        | mark-read / mark-all-read flows return `500` under authenticated use                             |
| LF-06   | LF-06-S03 | client/admin       | notification item           | Notification item click                                | `handleNotificationClick` in `src/components/NotificationCenter.tsx`                                                                                | Mark item read and route user to action                   | optional navigation to target             | `notificationsAPI.markRead` + UI navigate                                      | API + UI          | `e2e/api/api-notifications.e2e.ts`                                    | `fail`        | Role-negative send-notification test returns `401` where `403` expected                          |
| LF-06   | LF-06-S04 | client/admin       | notification popover footer | View Full History                                      | inline `onClick` navigate in `src/components/NotificationCenter.tsx`                                                                                | Route to full notification history page                   | `/notifications`                          | none                                                                           | manual            | code mapping                                                          | `not_started` | Route existence should be verified                                                               |
| LF-07   | LF-07-S01 | admin              | reconciliation overview     | New Settlement Run                                     | inline `onClick={() => setShowCreateDialog(true)}` in `src/pages/AdminDashboard/components/Reconciliation/SettlementRunsList.tsx`                   | Start settlement run creation workflow                    | create-run dialog                         | none until submit                                                              | static + manual   | code mapping                                                          | `not_started` | UI entry path mapped                                                                             |
| LF-07   | LF-07-S02 | admin              | create settlement dialog    | Create & Process                                       | `handleCreateAndProcess` in `src/pages/AdminDashboard/components/Reconciliation/SettlementRunsList.tsx`                                             | Orchestrate create -> process -> settle pipeline          | run appears processed/settled             | `create_settlement_run` + `process_settlement_run` + `mark_settlement_settled` | API + DB          | `e2e/api/api-reconciliation.e2e.ts`                                   | `fail`        | Role-negative assertions return `401` where test contract expects `403`                          |
| LF-07   | LF-07-S03 | admin              | settlement runs list        | Refresh                                                | inline `onClick={() => refetch()}` in `src/pages/AdminDashboard/components/Reconciliation/SettlementRunsList.tsx`                                   | Reload settlement list                                    | refreshed table                           | `get_settlement_runs` RPC via hook                                             | static + manual   | code mapping                                                          | `not_started` | Add explicit UI assertion                                                                        |
| LF-07   | LF-07-S04 | admin              | settlement runs list        | View run details (Eye)                                 | inline `onClick={() => setSelectedRunId(run.id)}` in `src/pages/AdminDashboard/components/Reconciliation/SettlementRunsList.tsx`                    | Inspect run internals before action                       | details dialog open                       | `get_settlement_run_details` RPC via hook                                      | manual + API      | code mapping                                                          | `not_started` | Validate detail payload integrity                                                                |
| LF-08   | LF-08-S01 | admin              | user audit log              | Refresh                                                | `fetchAuditLogs` in `src/pages/AdminDashboard/components/UserManagement/UserAuditLog.tsx`                                                           | Reload audit entries with current filters                 | updated audit entries list                | `audit_logs` table query                                                       | API + manual      | `e2e/api/api-audit.e2e.ts`                                            | `fail`        | Repeated role-negative mismatch (`expected 403`, `received 401`) in audit endpoints              |
| LF-08   | LF-08-S02 | admin              | user audit log              | Export Logs                                            | `handleExportLogs` in `src/pages/AdminDashboard/components/UserManagement/UserAuditLog.tsx`                                                         | Export filtered audit records                             | CSV download                              | client-side CSV serialization from loaded records                              | manual            | code mapping                                                          | `not_started` | Validate export schema/field completeness                                                        |
| LF-08   | LF-08-S03 | admin              | user audit log              | Entry details (Eye)                                    | inline `onClick={() => setSelectedEntry(log)}` in `src/pages/AdminDashboard/components/UserManagement/UserAuditLog.tsx`                             | Inspect full audit entry and field diffs                  | modal opens with full details             | none                                                                           | manual            | code mapping                                                          | `not_started` | Validate change diff rendering                                                                   |
| LF-09   | LF-09-S01 | client             | `/budget`                   | initial page load                                      | `loadData` in `src/pages/BudgetTracker.tsx`                                                                                                         | Hydrate budget dashboard data                             | cards/charts/tables populate              | `financeService.getTransactions/getBudgetOverview/getSavingsGoals`             | manual + unit     | code mapping                                                          | `not_started` | Service currently uses mock-backed behavior                                                      |
| LF-09   | LF-09-S02 | client             | `/budget`                   | CSV upload zone (`csv-upload-zone`)                    | `handleDrop`/`handleFileChange` -> `processFile` in `src/pages/BudgetTracker.tsx`                                                                   | Import bank statement and refresh metrics                 | updated budgets/transactions              | `financeService.processCSVUpload` + reload                                     | manual + e2e      | code mapping                                                          | `not_started` | Add dedicated E2E around supported CSV formats                                                   |
| LF-09   | LF-09-S03 | client             | `/budget`                   | Add Funds button (`+ Add Funds`)                       | `handleAddFunds` in `src/pages/BudgetTracker.tsx`                                                                                                   | Should contribute to savings goal funding workflow        | add-funds modal opens                     | `financeService.addToSavingsGoal()`                                            | static + manual   | `FLOW-FIX-BUDGET-001` implemented in `fix/responsive-ui-enhancements` | `pass`        | `handleAddFunds` wired to `financeService.addToSavingsGoal()`; fixed in `FLOW-FIX-BUDGET-001`    |
| LF-09   | LF-09-S04 | client             | `/budget`                   | New Goal button (`add-savings-goal`)                   | `handleCreateGoal` in `src/pages/BudgetTracker.tsx`                                                                                                 | Should create a new savings goal                          | create-goal modal opens                   | `financeService.createSavingsGoal()`                                           | static + manual   | `FLOW-FIX-BUDGET-002` implemented in `fix/responsive-ui-enhancements` | `pass`        | `handleCreateGoal` wired to `financeService.createSavingsGoal()`; fixed in `FLOW-FIX-BUDGET-002` |
| LF-09   | LF-09-S05 | client             | `/budget`                   | Filter/Export buttons (transactions card)              | filter state + `handleExportTransactions` in `src/pages/BudgetTracker.tsx`                                                                          | Should filter/export transaction list                     | filter modifies list; export triggers CSV | client-side CSV export from loaded records                                     | static + manual   | `FLOW-FIX-BUDGET-003` implemented in `fix/responsive-ui-enhancements` | `pass`        | Filter state + `handleExportTransactions` CSV export implemented; fixed in `FLOW-FIX-BUDGET-003` |

---

## Static Gap PR Tasks

- See `docs/FLOW_FIX_PR_TASKS_2026-02-15.md` for owner/severity assignment and acceptance criteria.
- `FLOW-FIX-IPS-001`
- `FLOW-FIX-BUDGET-001`
- `FLOW-FIX-BUDGET-002`
- `FLOW-FIX-BUDGET-003`

---

## Evidence Standards

Each `pass`/`fail` row must include:

- a reproducible test reference (test file or manual steps)
- observed result
- backend confirmation (table/RPC/state transition where relevant)
- file reference for handler/function intent

---

## Review Cadence

- Update this matrix per PR affecting flow logic.
- Publish summary report after each full-cycle validation:
  - `docs/FLOW_VALIDATION_REPORT_YYYY-MM-DD.md`
