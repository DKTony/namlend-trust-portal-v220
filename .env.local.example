# NamLend local environment template for LIVE Supabase testing
# Copy this file to .env.local and fill in your production Supabase values
# IMPORTANT: Do NOT commit .env.local

# Supabase (required)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Admin client (leave disabled; never expose service role to the browser)
VITE_ALLOW_LOCAL_ADMIN=false
# VITE_SUPABASE_SERVICE_ROLE_KEY=   # DO NOT set this unless intentionally debugging locally. Never commit.

# Optional app metadata
VITE_APP_VERSION=2.2.2
VITE_APP_ENVIRONMENT=production
